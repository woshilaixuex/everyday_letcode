#include <bits/stdc++.h>
using namespace std;

int main() {
    int n = 0,k = 0;
    cin >> n >> k;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    
}
// 64 位输出请用 printf("%lld")